/**
 * TTS — Text → Speech
 * V1 runtime: Piper (local)
 * Wired in Phase 5.
 */
export async function synthesizeSpeech(_text: string): Promise<Buffer> {
  throw new Error("TTS not implemented — Phase 5");
}
